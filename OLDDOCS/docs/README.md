# Data Directory

This directory will contain:
- `config.json` - System configuration
- `users.json` - User accounts and preferences
- `backups/` - Automatic and manual backups

**Note:** In Docker, this maps to `/config` for Unraid compatibility.
